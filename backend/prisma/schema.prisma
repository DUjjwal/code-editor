generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model User {
  id          String @id @default(cuid())
  name        String
  first_name  String
  family_name String
  email       String @unique
  picture     String?
}
